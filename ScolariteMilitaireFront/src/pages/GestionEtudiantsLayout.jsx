import { Outlet } from 'react-router-dom';

/** Enveloppe des sous-sections « Gestion des étudiants » — contenu inchangé via Outlet. */
export default function GestionEtudiantsLayout() {
  return <Outlet />;
}
